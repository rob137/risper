# Publish

Queued publish request:

- GitHub owner: `rob137`
- repository: likely `risper`
- create a local git repo if needed
- push only after rename validation passes
- keep generated runtime data out of git:
  - `~/.local/share/risper`
  - `~/.config/risper`
  - `~/.local/state/risper`
  - model caches and virtual environments
