# issues

Known problems, written up but not yet worked. One file per problem, named for
the symptom rather than the suspected cause. They state the pressure and leave
the fix open; a file moves to `archive/` once it is closed out.
